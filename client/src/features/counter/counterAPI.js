export function fetchCount(amount = 1) {
  return new Promise(async (resolve) => {
    const response = await https://deploy-mern-server.vercel.app')
    const data = await response.json()
    resolve({ data })
  });
}
